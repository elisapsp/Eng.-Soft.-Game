using UnityEngine;
using System.Collections;

public class ExitGame : MonoBehaviour {

    public void exitGame()
    {
        //Na realidade, deverá voltar ao menu inicial do jogo, que ainda não foi feito.
        Debug.Log("Deverá mudar para voltar ao menu inicial do jogo, que ainda não foi feito.");
        //Temporário.
        Application.Quit();
    }

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
